name: Update Kikugawa Tenki

# 外部サイト(cron-job.org)から
# POST https://api.github.com/repos/<owner>/<repo>/actions/workflows/kikugawa-update-tenki.yml/dispatches
# を叩いて起動する(kikugawa-update-wbgt.ymlと同じ方式)。
# scheduleは外部トリガーが止まった時の保険として30分毎に残す。
on:
  workflow_dispatch: {}
  schedule:
    - cron: '*/30 * * * *'

permissions:
  contents: write

concurrency:
  group: update-kikugawa-tenki
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # kikugawa_tenki.py は標準ライブラリのみ使用のため pip install 不要

      - name: Run Tenki script
        run: python kikugawa_tenki.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add kikugawa_tenki.json
          git diff --staged --quiet || git commit -m "Update kikugawa_tenki.json [skip ci]"
          git push
